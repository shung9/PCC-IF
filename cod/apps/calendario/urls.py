from django.urls import path
from . import views
#from .views import google_auth, google_auth_redirect, google_calendar

urlpatterns = [
  #  path('google-auth', google_auth),
   # path('google-auth-redirect', google_auth_redirect),
  #  path('google-calendar', views.google_calendar, name="google-calendar"),
   # path('', views.ver, name="ver"),
]
